


const PetObject = {
    food: 'fish',
    color: 'black',
    size: '55.5 cm',
    age: 2,
};

const {
    food,
    color,
    size,
    age
} = PetObject;

const name = 'Cat';

const temString = ` This is my ${name}. It is ${age} years old and color is ${color}.
`;

console.log(temString);



